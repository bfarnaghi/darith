# API Status

Darith currently uses authenticated Django pages and forms. The old token API was removed because it did not enforce user ownership consistently.

No public API is included in this version.
